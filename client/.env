# This setting tells the development server to allow requests from any host.
# It is generally safe for local development but be aware of the security
# implications if you expose your development server to other networks.
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Disable source map generation to hide source code in production and suppress
# warnings from third-party libraries with broken source map paths.
GENERATE_SOURCEMAP=false